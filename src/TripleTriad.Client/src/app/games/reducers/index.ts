import {
  ActionReducer,
  ActionReducerMap,
  createFeatureSelector,
  createSelector,
  MetaReducer
} from '@ngrx/store';

export interface GameState {}

export const reducers: ActionReducerMap<GameState> = {};
